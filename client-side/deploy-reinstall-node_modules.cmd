@echo off
rem lastmodify
rem 2019-02-19:create, 移除并重新安装生成环境所需的npm模块

echo 移除并重新安装生产环境所需的npm模块
echo.

rem 删除node_modules文件夹
echo 正在移除npm模块（请勿关闭窗口）
if exist %cd%\node_modules (
	rmdir /s/q %cd%\node_modules
)
echo 移除npm模块成功
echo.

echo 正在重新安装npm模块（请勿关闭窗口）
call cnpm install --production
echo npm模块安装完成
echo.

pause
